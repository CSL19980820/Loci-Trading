# 龙头选手观察池与通信链路验收（2026-09-17）

## 当前生产版本

镜像：`loci-qianlong:2.0.0-20260917-leader-watch5`。

镜像摘要：`sha256:658ac2a19387d6b54cb30bfc157f3f13135811a26ae142c95c228a83bd41fb58`。

正式发布源：`/srv/qianlong-loci/releases/2.0.0-20260917-leader-watch5/`。以已上线的 `grpc-all` 镜像为基底，保留生产已有接口和其他配置，只定向修改本次相关文件。该目录中的源码与 `manifest.json` 是本次镜像的复现与核验依据；本地工作区还存在其他未发布改动，不能把整个工作区视作正式版本。

部署入口沿用 `server-deploy.sh` 的摘要校验、健康检查、Nginx 检查与失败回滚，不重新下载依赖、不导出大镜像。2026-09-17 23:55 北京时间核验：容器 healthy，公网健康接口与原报告地址均返回 HTTP 200，入口协商 HTTP/2。

## 生效规则

| 阶段 | 查询和决策范围 | 观察池调整 | 成交权限 |
|---|---|---|---|
| 当日盘后复盘 | 可研究全市场并准备次日名单 | 可增加、替换、撤回 | 仅分析，无模拟成交 |
| 次日盘前 | 可重新比较并确认当日名单 | 可增加、替换、撤回 | 仅分析，无模拟成交 |
| 竞价 | 已确认观察股与已有持仓 | 不新增对象 | 保持原有仅研判设置 |
| 盘中、尾盘 | 仅实际持仓；空仓时范围为空集 | 不增删、替换观察股 | 可持有、减仓、止盈止损及退出；不得新开仓或加仓 |

观察池上限为 **5 只未持仓股票**。实际正持仓数量大于零的股票不进入观察池，不占名额。退出持仓不自动重新加入观察池，后续盘后或盘前再决定。完整批次按最终名单检查上限，因此“先添加新票、再撤回旧票”的换股顺序不会制造临时第六只导致误拒。

这是容量上限，不是必须选满5只。该版本没有新增自动开仓时段；观察名单不会自动触发买单。盘中仅管理持仓的限制优先于历史日记、旧观察计划或提示词中的自由选股描述。

约束同时落实在提示词、工具目录、查询参数检查、最终行情拉取、模拟决策和存储提交层；直接绕过模拟器提交盘中新增买入或改变观察池同样被拒绝。保留账户守恒、T+1可卖股数及原有资金和持仓数量限制。

在实际发布包测试中还发现旧成交路径可把没有对手盘的报价视作成交。股票智能体路径现在在已提供盘口但对手价或数量无效时明确拒绝，并保留盘口回执；没有据此宣称已修复整个旧交易引擎的所有问题。

## 已有实例迁移

目标：`agent-b0b801c77a3e4f6992c413277f97965a`，名称“龙头选手”。

2026-09-17 23:53:02 北京时间：配置版本 **11 → 12**，`watch_limit` **3 → 5**。仅替换与旧内置模板完全一致的盘中、盘前、复盘提示词；公共策略提示词保留。迁移前确认没有在途运行，并通过配置版本检查避免覆盖并发修改。

`daily_selection_limit=0`、`position_limit=2`、`temporary_position_limit=2` 均保持不变。原3只观察股与1只持仓保留，不新增选股、不重做复盘、不发送通知；数据库中的账户状态、状态版本、交易数、运行数与最新状态逐字段确认未变。

线上盘中查询范围实测为 `600176`；盘后及盘前允许重新研究，竞价范围为观察池和实际持仓。日程保持 20:00 复盘、08:50 盘前、09:25 竞价、盘中每5分钟。

迁移前私有备份保留在：
`/srv/qianlong-loci/data/maintenance_backups/leader-before-watch5-20260917-235302.json`，文件权限600。

恢复配置应使用该备份中的配置并核对当前版本，不应覆盖后来产生的真实账户状态或成交记录。上一版镜像和部署前 `.env` 备份保留；回退旧镜像前需同时考虑旧版本观察上限校验，不能只改镜像而忽略新配置。

## 前端

`AgentConfigDrawer.vue` 中龙头“同时观察上限”最大值同步调整为5，自定义智能体范围不变。正式前端只修改当前已发布组件的该数字边界，给受影响的加载依赖生成新资源名以更新缓存，没有夹带本地未发布的整套前端改造。静态资源对应关系见发布目录的 `frontend-patch.json`。

浏览器验收使用**实际发布的 JavaScript 与隔离接口夹具**：设置框能输入5，`aria-valuemax=5`，0个缺失资源、0个页面异常。这不是使用伪造登录访问生产数据库；生产配置另行通过实际运行容器与数据库核验。

原分享报告另以无夹具浏览器直接访问正式站点：HTTP 200，标题“2026-09-17 · 晚间复盘 · 龙头选手 · Loci”，可读正文4793字符。部署后再次HTTP请求确认12761字节HTML，原链接未更换。历史快照保留当时3只观察股的原文，不伪改历史报告为新规则。

## gRPC 全量验收补完

业务模型配置入口使用 `LOCI_LLM_GRPC_MODE=all`，网关 `LOCI_GRPC_MULTI_TENANT=1`，端点为容器回环地址 `http://127.0.0.1:50051`。助手、股票智能体及其他使用统一模型配置入口的业务调用默认经过gRPC网关。浏览器接口、第三方原生接口不会因此成为原生gRPC。

本轮在正式容器以网页应用UID执行真实流式调用，探针显式关闭HTTP回退，以响应元信息核实 `transport=grpc`，网关上游均为HTTP/2：

| 配置 | gRPC真实模型结果 | 对照/解释 |
|---|---|---|
| B.AI · 守护 / deepseek-v4.1-flash | HTTP 200，正文OK，43 tokens | 整轮1888ms，仅单次功能样本 |
| GLM / glm-5.3-flash | HTTP 200，正文OK，107 tokens | 整轮1975ms，仅单次功能样本 |
| deepseek / deepseek-v4-flash | 上游401，未计为成功 | 独立原HTTP直连也返回401，属于供应商鉴权问题 |

这些结果不证明gRPC比HTTP更快。deepseek配置需要在供应商设置中提供有效API Key，本次未替换密钥、未偷偷换用其他账户，也未将其鉴权失败隐藏成链路成功。

新镜像重启后，3个配置的gRPC路由及**不计费Check就绪检查**均通过；Check只验证网关与配置可解析，不代表上游密钥有效。通信源码未在观察池发布中改动。

兼容保留：仅请求提交前Check不可用时允许按配置回退HTTP；已提交请求不因换协议而重放；鉴权错误不降级绕过。网关到供应商保持HTTP/2优先、HTTP/1.1兼容。`LOCI_LLM_GRPC_MODE=off`配合重新创建容器可恢复原HTTP业务路由。

## 验证范围与剩余事项

本轮股票智能体相关回归 **90项通过**；实际发布源码复测 **46项通过**。发布包检查没有纳入3个针对本地尚未发布SQL研究能力的用例；这些用例仍保留在本地完整回归中，没有将其算作生产通过。生产镜像内导入与5只配置模型校验通过，231个发布文件摘要与运行容器逐一匹配。

全项目Ruff F通过；导入冒烟680个模块、0失败。全目录架构依赖检查仍有3处跨层引用，不计为通过：`guardian_tools -> market.infrastructure.adapters.tencent_adapter`、`guardian_research_bootstrap -> ai.infrastructure.client`、`app.main -> ai.infrastructure.grpc_gateway`。这些文件未随本次观察池调整重构，后续可按公共模块入口整理。

23:55检查根分区剩余约276MB，容量风险仍存在。本次只清理本轮及上一轮明确生成的临时脚本、上传包、隔离副本和截图，不批量删除生产数据、历史镜像或回滚备份。

清理已核验：本地两轮临时目录均不存在，服务器9个命名临时文件与容器内2个验证文件已删除；正式发布源、摘要清单、镜像及回滚备份保留。清理后容器仍healthy，根分区剩余约283MB，容量风险未消除。
