# 交易员开盘风险取价恢复

## 13:00 轮次的原始证据

2026-09-17 13:00 正式轮次在 13:00:06.284 对四只持仓的八份风险合同记录了 `quote_unavailable`。初次取价耗时 6.176 秒，模型研究 77.154 秒，最终预检 1.043 秒；没有成交，最终状态 failed。

同轮保存的 `guardian_quotes` 原始回执显示：13:00:10.630 时四只持仓已经有腾讯实时行情，行情时间为 13:00:06，`quote_errors={}`；13:01:17 的预演又取得 13:01:15 的新浪行情。程序仍将初次风险缺价事件转为最终拒单并发送执行受阻通知。这八条不是八次已确认触发的止盈/止损订单，而是四只股票的两类合同分别缺少有效监测行情。

旧版本没有保存初次持仓取价的完整原始报价，不能断言首批报价的精确时间；“开盘上游快照尚未更新”符合现场恢复过程，但不能把它当作已经保存的原始报价事实。

## 修复

9:30 与 13:00 开盘前 30 秒内，首批持仓报价未通过校验时，在原有 30 秒取价预算内重新获取无效代码的行情；已有有效报价不重复获取。每次查询都强制刷新，使用真实行情时间，维持取消和期限检查。

只有取价恢复或预算耗尽后才评估风险合同。恢复后确实触发的风险合同仍绕过模型直接进入原风险执行链路；没有触发则正常研究，不把一次可恢复的瞬时缺价留成最终拒单。持续缺价仍然报错，不在最终阶段掩盖长期监测缺口，也不通过改时间戳、放宽新鲜度或重写历史状态使其通过。

新增 `holding_quote_attempts` 保存每次检查时间、完整报价及缺价代码；正常结束、失败和取消均保存这些证据。13:00 旧记录保持原样，不补造当时缺失的数据。

## 验证

风险执行、执行安全和报价验证共 113 项通过；新增覆盖两个开盘边界上的旧报价恢复、恢复后触发止损、恢复后无需交易、取价预算耗尽仍拒绝、恢复等待可取消并保存证据。静态检查通过。

## 发布与正式轮次

等待旧版本的 13:05 正式轮次完成后发布；该轮 success/no_action，无拒单，未被部署中断。

新镜像 `loci-qianlong:guardian-opening-20260917`，ID `sha256:b416bb6273b281ebed3112710659e7e2f4a7b6484713efa00e3b9d06473843a3`，基于实际线上镜像仅叠加 `src/ops/application/jobs/guardian.py`。与线上原文件比较确认差异仅为本次修复。容器及健康接口正常，本地/容器文件 SHA-256 均为 `1b8b41ef1250d6eb1424aa7518372ca842f07178e0904847c4174d64cc3ba7e0`。回滚配置 `backups/guardian-opening-20260917.env`，构建目录 `/tmp/loci-guardian-opening-20260917` 已删除并核验不存在。

13:10 正式轮次在 13:10:01.792 的初次持仓取价中四只股票均通过验证，来源为悟道，行情时间 13:09:00，`unavailable_codes=[]`；完整报价已经记录到 `holding_quote_attempts`。

该正式轮次已完成：`status=success`、`outcome=no_action`、`rejects=[]`、`fills=[]`，通知回执 `skipped=no_action`，没有发送执行受阻通知。持仓取价 1.770 秒、研究 128.462 秒、最终预检 1.511 秒。它验证的是新版在实际交易时段的正常完整链路；9:30/13:00 首批缺价后的恢复分支由上述回归用例验证，不能将 13:10 冒充开盘边界实测。
