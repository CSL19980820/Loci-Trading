# 线上选股性能修复验证 · 2026-09-12

截图对应 `impulse-pullback-tail-v1`，请求窗口 2026-08-13 至 2026-09-12；实际覆盖截至 9 月 11 日的 22 个交易日。股票池为主板和创业板，剔除 ST、退市及停牌标的，上市不少于 60 日；参数 DF=0.1、TS=10、SL=0.8、ZF=0.03。

## 线上完整请求结果

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 后台完整任务耗时（含准备与入库） | 1166.917 秒 | 202.107 秒 |
| 每个交易日选股耗时中位数 | 49.941 秒 | 8.599 秒 |
| 完成交易日数 | 22 | 22 |
| 入库候选记录 | 8 | 8 |

完整任务约快 5.77 倍，耗时减少 82.7%。这是原用户任务与部署后同参数复跑的线上观测，并非相同冷缓存状态下的独立基准；修改前运行期间做过短暂的只读性能采样。

验证通过真实 `POST /api/screen/run` 启动，轮询至 `status=done`、`percent=100`，逐日股票范围、正式/观察数量一致。再从 `GET /api/screen/history?live_only=false` 回读，8 条记录的股票、日期、候选池、裁决、入场时点、参数及全部因子完全一致。重放自然更新记录 ID 和创建时间；这两项不作为算法一致性依据。

## 瓶颈与改动

1. 战法逐股票、逐 K 线执行 Python 循环，完整前视检查又重复计算。改为每批 128 只股票的数组计算，按各票真实 K 线压缩停牌、恢复原日期，保留公共 ZTPRICE 和相同窗口的归约顺序；临时归约窗口限制 65536 个元素。
2. 热库完整性检查原先为统计约 700 个日期扫描数百万条股票索引。改为按日期主键跳到下一日，仍检查实际日 K，日历存在不能掩盖缺失行情。
3. 增加“同步选股热库”和“检查前视偏差（全股票范围）”进度。浏览器已验证阶段文字和最终完成状态。

没有取消体检、前视检查、逐日回放或候选入库保护，也没有修改策略阈值和复权口径。

## 线上真实行情的算法对照

只读打开服务器热库，分别加载 120 根 K 线；原线上模块与待发布模块在同一进程内先后计算，使用 `check_exact=True` 对照所有日期、股票与因子。

| 窗口末日 | 股票数 | 原计算秒数 | 新计算秒数 | 结果 |
|---|---:|---:|---:|---|
| 2026-08-13 | 4394 | 10.736 | 0.725 | 24 个因子与全部信号严格相等 |
| 2026-08-28 | 4396 | 13.813 | 0.623 | 24 个因子与全部信号严格相等 |
| 2026-09-11 | 4397 | 11.495 | 0.637 | 24 个因子与全部信号严格相等 |

## 发布与回归

- 线上原版本：`loci-qianlong:2.0.0-20260912-053308`。
- 本次版本：`loci-qianlong:2.0.0-20260912-173709`，容器 healthy，域名 health 正常，4 个修改的 Python 文件 SHA256 已与发布包核对。
- 通过标准 `deploy/deploy.ps1 -SkipFrontendBuild -Mode Bundle` 发布；由于工作区有大量其他并行改动，使用原线上源码与原前端产物构造隔离发布目录，只应用本轮 4 个代码文件、2 个 README 的差异。
- 本地另有战法目录下线改动，不属于本轮性能修复；部署保留原线上注册，未回退本地其他工作。优化代码由本轮 Git 提交记录，其他工作区改动保留。
- 隔离发布源码上 **200 项相关测试通过**，含战法、全部因子精确对照、停牌与坏行、浮点相邻边界、完整前视检查、多任务隔离、热库回退、历史入库范围。
- 变异验证：将 `SX <=` 改为 `<` 后边界用例失败；恢复旧全索引计数后 SQLite 指令预算用例失败；恢复修复后测试通过。
- `ruff F`、真实模块导入通过；跨上下文导入检查 14 项契约通过。提交由本轮交付环节记录。

发布后文件摘要：

- `src/market/infrastructure/store_hot.py`：`cc38396812ef7f86b8bd7a3401cb06dc9a24ca60685d3ca6873be0f610ade4e0`
- `src/strategy/application/impulse_pullback.py`：`fa2366a1ad2229f8b7ec760cf11e2591cb73684cc68e765310d9c9f692a2417f`
- `src/strategy/application/screen_run.py`：`757d31d540662fa46be43007a12a1201cb1eb05c66cc6c9b20a931f51cb9d366`
- `src/strategy/application/screener.py`：`8bfa52553f68167ee0ce1d3d024d168c1d6a8107c64d7a72fe3bcab92e1b7181`
