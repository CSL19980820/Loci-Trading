# 核心链路重构与性能验收

## 已交付范围

2026-09-09 已完成行情面板、策略审计、研究输入、成交引擎及性能门禁的重构，
部署到唯一线上环境 `my-debian`，镜像 `loci-qianlong:2.0.0-20260909-120300`。
未提交或推送 Git；未操作已搁置的便携版。

- 行情字段共用一次 pivot，消除逐字段索引编码与排序；Polars 按列转换，去掉逐行字典。
- 策略审计提前提取探针日的正式/观察代码集合，在下一次计算前释放前次因子矩阵。
  探针日期、全列分片覆盖、静态审计和阻断规则保持原样，测试覆盖信号消失分支的释放。
- 研究入口删除仅作 pandas/Polars 往返的 POC；旧函数保持兼容和输入 dtype，旧开关不再启用转换。
- 旧快速回测入口统一委托权威引擎，删除约 180 行重复成交逻辑，真实回显 classic。
  成本移出循环、信号坐标一次转换，无触价规则时跳过空扫描；未测得稳定的整体回测提速。
- 性能基线现在按版本化时间/内存上限拒绝退化，也拒绝缺项、样本不足、非有限数和失败样本。
  测试时钟固定到信号夹具时间，避免七天保留规则使测试随日期推移失败。

## 验证

- 工作区：1753 passed，189 subtests；行情、研究、策略、回测及运维。
- 隔离发布目录：941 passed，174 subtests，含独立发布源的核心回归及性能门禁。
- 导入冒烟：574 个模块，0 失败；依赖边界：14 kept，0 broken。
- 旧版本逐笔对照：2000×370 合成行情，4 种入场时点 × 4 种止损止盈组合，
  每组 10248–14812 笔交易，完整结果、跳过原因与指标一致。
- 现有新浪日期解析测试有一条格式推断 warning，不影响通过。

## 线上前后对照

只读打开生产行情库，在同一段 2025-03-01 至 2026-08-28 历史数据上，选取
256 只证券、366 根日线；各路径先预热一次，再测五次。均在生产容器内执行。
前后行情面板、正式信号及 26 笔回测完整结果的 SHA-256 相同：
`5e81aac599778968285329bca622f9b1f3b00c179cab44d8cb21694ac43d0e17`。
动态审计通过。7 个运行时代码文件 SHA-256 与已验证本地文件一致。

| 路径 | 改前 P95 / ms | 改后 P95 / ms | 本次观测下降 |
|---|---:|---:|---:|
| 行情加载 | 1076.81 | 806.56 | 25.1% |
| 潜龙计算 | 130.94 | 115.42 | 11.9% |
| 全分片审计 | 479.99 | 414.66 | 13.6% |
| 26 笔成交 | 3.89 | 3.71 | 4.6% |

这是有界历史样本的前后观测，受宿主负载、缓存及重启影响；不能把所有下降归因于代码，
也不能外推为全市场并发 SLA。核心成交的本地对照没有稳定提速，保留这一证据边界。
容器最终 running/healthy、restarts=0；域名 `/api/health` 返回 HTTP 200 和 `status=ok`。

## 性能保障的覆盖与边界

`tests/benchmarks/performance_budgets.json` 是资源上限入口；显式运行：
`python -m pytest -q tests/benchmarks/baseline_benchmark.py`。
原来的 256×240 数据库基线保留，新增生产潜龙算法的 2000×370 计算、全分片审计和
含止损的 14772 笔合成成交。预热后采样五次，进程 RSS 高水位上限为 768 MiB。
本地独立基线观测：计算 P95 717 ms、审计 1619 ms、止损成交 344 ms，进程峰值约 304 MiB。
这些预算是保守的回归天花板，不是客户响应时间承诺；本次未提交代码，未声称远端 CI 已运行。
仍需后续真实并发与长时间负载验证，才能给全市场、多租户和 AI 研究任务明确容量承诺。

## 发布隔离与回滚

发布基于线上 `2.0.0-20260904-123724` 的源码及前端产物，仅覆盖本轮 7 个后端文件
和对应说明。前端产物逐文件校验与线上原版本一致；工作区原有的运维、行数缓存及前端
未提交改动未混入发布。完整发布文件清单在 `output/system-refactor-manifest.json`。

如需回滚，按 `deploy/README.md` 将服务器 `.env` 的 `LOCI_IMAGE` 设回
`loci-qianlong:2.0.0-20260904-123724`，在 `/srv/qianlong-loci` 执行 `docker compose up -d`。
原始前后数据在 `output/system-refactor-live-before.json`、`output/system-refactor-live-after.json`，
线上文件与行为检查在 `output/system-refactor-live-files.json`。本轮未包含数据迁移；验收只读生产行情库。

临时产物清理：自动审批策略拒绝删除 output/system-refactor-release（明确单目录操作仍返回 blocked by policy）。发布目录及两份打包文件暂保留，未改用其他工具绕过；它们不参与线上运行。
