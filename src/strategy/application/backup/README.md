# 内置战法备份

此目录保存已从产品目录下线的旧版内置战法源码快照。目录不会被 `src.strategy` 自动导入，不能作为活动选股战法注册；如需复盘旧口径，应显式读取对应快照并单独运行审计。

- `qianlong-legacy.py`：`qianlong-close` 下线前源码
- `lugaowen-legacy.py`：`lugw-sanwai`、`lugw-haidi`、`lugw-chouma` 下线前源码
