akshare>=1.12.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
# market 只读分析旁路（可关；不替代 palace/market SQLite 权威）
duckdb>=1.0.0
# 可选回测加速（LOCI_BACKTEST_FAST=1）；缺省仍走经典引擎
vectorbt>=0.26.0
fastapi>=0.115.0
uvicorn>=0.30.0
httpx2>=2.9.0
itsdangerous>=2.2.0

# 定时任务调度（进程内单例，不引入 broker）
apscheduler>=3.10.0
# 技能包 SKILL.md 的 YAML frontmatter
PyYAML>=6.0
# API Key 静态加密（AES-256-GCM）
cryptography>=42.0.0

# 桌面窗口（Loci.exe / python loci.py）
pywebview>=5.0
# 任务栏托盘悬停行情提示
pystray>=0.19
Pillow>=10.0

# 可选：打 Windows exe
# pyinstaller>=6.0
