# 19 项界面与访客改造上线记录

状态：已完成前后端部署及验证。版本：`2.0.0-20260921-002839`。站点：`https://qianlong.chenkit.cloud/`。

## 发布范围与方式

对应 `ui-access-acceptance.md` 的全部 19 项改动，包括完整前端、后端只读访客权限、会话规则、身份数据迁移、公告/个人 API 退役和日志保留设置。

使用项目 `deploy/deploy.ps1` 重新构建前端、封装和上传完整发布包。服务器依赖镜像站下载缓慢，因此在尚未切换容器前停止了该次重复依赖下载。逐项确认 65 个已安装依赖与本次锁定文件完全一致，且 `pip check` 通过后，复用原运行环境生成新的完整应用镜像；所有 `src`、`cli`、`templates`、`frontend/dist` 均整目录替换，移除旧 `FRONTEND_RELEASE.json`。随后使用项目 `server-deploy.sh` 的镜像加载分支完成容器切换、Nginx 验证和回收。不是只覆盖前端，也没有上传本地账本覆盖生产库。

运行镜像：`loci-qianlong:2.0.0-20260921-002839`。

镜像 ID：`sha256:a54352491a772a57d4a1e73bef897a6fc1e858d83932af5e585499fed9f68afe`。

## 生产验证

| 检查 | 结果 |
| --- | --- |
| 容器与 Nginx | healthy，域名健康检查 200，Nginx 配置测试通过。最终检查重启计数 0，新容器启动以来 ERROR/Traceback 匹配为 0。 |
| 公网 HTTP / HTTPS | 两个入口均通过健康检查、未登录会话和受限接口鉴权；桌面/手机登录页共 4 个场景通过。 |
| 发布内容 | 1022 个运行文件与本地发布包逐字节哈希一致；公网 275 个静态资源逐个验证成功。 |
| 身份迁移 | `access_model_admin_visitor_v1` 已落地，公告和个人 API Key 表不存在，完整性检查通过。 |
| 账号与配置 | 原账号标识、租户、用户名、密码哈希、状态与备份一致；环境配置除部署镜像/端口字段外保持不变。 |
| 财务与候选数据 | 9 张关键表完整内容与部署前备份一致，含 29 条自主交易员成交、2 条股票智能体成交、143 条候选记录和 171 条股票记录。 |
| 非 root 运行 | 应用进程 UID 为 10001。 |

## 同一发布镜像的隔离真实接口验证

镜像 ID 与最终生产容器严格一致。使用独立临时数据和随机测试账号，关闭定时调度，不挂载生产数据库，不用 API mock。

8 组检查、14 个管理员桌面/访客手机页面场景通过，浏览器运行错误为 0。覆盖真实登录、访客授权工作区读取、后端拒绝写入及私密设置、访客新会话撤销旧会话、旧页面退出、管理员多端并存、日志保留保存/重读/版本冲突，以及退役接口拒绝访问。界面检查关闭新数据环境的行情初始化弹窗后拍摄页面。

生产站点验证的范围是公网未登录读取、静态资源及服务端发布/数据核对；登录后的业务交互和权限写入场景使用同一生产镜像的隔离环境，不冒充在线上真实账号中执行的测试。隔离容器与隧道已清理。

## 备份与回滚

部署前一致性备份：`/srv/qianlong-loci/backups/ui-access-predeploy-20260921-002707`。包含身份、账本、运维、社区及报告分享库，生产配置、compose 和战法文件；行情历史库没有随本轮身份迁移重建或覆盖。

身份迁移自动备份：`/srv/qianlong-loci/data/.backups/identity-before-visitor-20260920T164622Z-245597.db`。

上一版镜像 `loci-qianlong:ui-20260920-152649` 已确认仍可用。本轮身份表结构已迁移，不能假定仅切回旧镜像就能完整回退。确需回退时，应先停止写入并另做当前一致性快照，在维护窗口按旧版本匹配身份备份处理 WAL/SHM 后恢复；不能直接在线覆盖数据库，也不能把旧账本快照覆盖之后新增的业务记录。

## 验收证据

服务器：`/srv/qianlong-loci/releases/2.0.0-20260921-002839/DEPLOYMENT_VERIFIED.json`。

本地：`.local/ui-access-20260920/DEPLOYMENT_VERIFIED.json`；该目录中的 `final-deployment.log`、`runtime-verification.json`、`public-ui/receipt.json`、`isolated-ui/receipt.json` 和对应截图均为本轮保留的部署/验证证据。`deployment.log` 记录首次依赖下载被主动中止，最终成功结果以 `final-deployment.log` 为准。
