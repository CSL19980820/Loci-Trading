# 设置清理与界面修复发布验收

发布标识：`ui-20260921-014043`。实际切换时间：2026-09-21 09:42:04（北京时间）。
生产目录：`/srv/qianlong-loci`；应用容器：`qianlong-loci`。

## 本轮交付

设置页移除已不适用于服务器网页版的数据目录迁移、托盘／窗口设置、重复外观设置和 Loci.exe 一键打包入口，并停止调用相应的桌面目录及打包状态接口。主题入口仍在头像菜单。账号列表移除租户展示和已退役的资源额度；保留账号独立数据归属。清理任务使用“运行日志清理”展示名称，实际权限和数据隔离不变。

同时完成大模型用量列表／曲线／面积视图、全宽胜率与证据切换、周一日期键对应的整周样本筛选、候选池手机布局和北京时间秒级显示、档案手机行情与 K 线读数布局、原生手机代码编辑器、选股工具栏、设置导航和弹窗布局修复。未保存设置离开时可取消，取消后保留草稿。

## 验证结果

- 生产构建包含 vue-tsc 检查，通过。构建仍有原有大型异步编辑器分片体积警告，不属于构建失败。
- 24 项单元测试通过：北京时间格式 10 项；月份／整周／跨年周期匹配 14 项。
- 实际构建产物在 Edge 浏览器完成 48 项检查，覆盖 1440×900、1920×900 与 390×844。页面脚本错误、接口错误和废弃目录／打包接口调用均为 0。
- 浏览器登录和设置接口使用独立测试数据库；行情、候选与用量展示使用明确标注的合成夹具，不代表生产业务数据。
- 28 项隔离接口检查在本地通过，并在实际候选镜像中再次通过。镜像检查不挂载生产数据卷、禁用网络，使用临时账号及数据库。
- 三个指定后端文件的 Ruff F 检查通过。
- 容器运行健康状态为 healthy；直连应用与 nginx 返回的 HTML 与发布文件完全一致。
- 公网 HTTPS 登录页 HTML 与 74 个入口／本轮页面静态资源逐项 SHA-256 一致；公网健康接口 HTTP 200。此检查未登录真实生产账号。

## 发布边界

发布前检测到另外两个更新已经上线，因此以最新 `quote-20260921-093541` 镜像为基础，保留同期的经验沉淀和行情修复。此次只覆盖前端产物与以下三个后端源文件：

- `src/identity/api/admin_router.py`
- `src/identity/application/accounts.py`
- `src/identity/infrastructure/store_platform.py`

这三个文件增加登录客户端采集、登录方式服务端过滤，并把认证事件与配置／权限审计在界面查询中分离。未更改数据库结构。

除上述三个文件外，运行镜像的 src、cli、templates、requirements 文件指纹前后一致：
`ff1be8fbe27a5392b8b40b4c565d1088b4b695e332c21dff512d3aef2b6a6cb0`。
部署只调整运行镜像标识，其他配置及 docker-compose.yml 未更改。未重置真实账号密码，未迁移或主动清理业务账本、持仓或成交。发布未执行生产业务写入测试。

## 发布资料与回退

服务器发布目录：`/srv/qianlong-loci/releases/ui-20260921-014043/`。
其中保留 `FRONTEND_RELEASE.json`、`DEPLOYMENT.json`、`API_SMOKE.log`、完整前端源码快照及测试证据。公网验证回执为 `PUBLIC_VERIFICATION.json`。

当前镜像：`loci-qianlong:ui-20260921-014043`。
镜像 ID：`sha256:1274e209e8b0a1996e207efd991c21b13ea3f6fdb6778b629d62a60018d32401`。
回滚镜像：`loci-qianlong:before-ui-20260921-014043`，保留发布前的经验沉淀与行情修复。

需要恢复运行版本时，仅切换镜像，不回滚或删除数据库：

```bash
cd /srv/qianlong-loci
python3 releases/ui-20260921-014043/release_check.py pin .env loci-qianlong:before-ui-20260921-014043
docker compose up -d --no-deps qianlong-loci
```

回退后仍须检查容器健康与网页入口。以上回退命令未执行。
