# 手机近选跟踪完整详情优化

2026-09-21 18:39（北京时间）上线，版本 `ui-20260921-183734`。

手机列表补齐 T+1、T+3、低→高、选入价、最新价及累计涨跌。点击卡片展开完整选股日、战法和取数口径，提供真实个股档案入口，保留本批切票上下文。缺失值显示 —，不替换成零；桌面表格和后端取数计算未改动。

## 验证

- 当前源码及隔离候选源码的类型检查、生产构建均通过。
- 360、393、430 手机宽度和 1440 桌面宽度的浏览器检查通过，覆盖深色、长名称、缺失值、展开/收起、档案路由、本批上下文和空态。隔离截图使用明确标注的合成数据。
- 线上已登录 Chrome 的 393×844 视口检查了真实 13 条跟踪记录。国科微一行 T+1 +0.41%、T+3 +15.76%、低→高 +18.94%，选入 173.64、最新 201.00，与发布前桌面表格一致。
- 实际点击展开详情，再进入国科微档案，显示真实最新价、行情指标和 K 线；本批位置 9/13。返回列表后展开状态保留。恢复桌面视口后表格正常，无页面脚本错误。
- 公网入口及静态文件共 281 项 SHA-256 一致，健康接口 ok，容器 healthy；直连与 nginx 的入口文件一致。

## 发布隔离

从已部署源码对应的历史快照构建，只加入 PulseTrackTable.vue 的新详情和已经上线的 MobileBottomNav.vue 键盘修复，未发布另一项并行任务尚在验收的研判、曲线或助手修改。隔离构建的 JavaScript 在本次修改前经资源文件名归一化后，与 settings-cache-20260921-174300 的内容匹配。迁移构建目录导致 Tailwind 多扫描了额外工具类，因此最终产物保留当前线上完整全局 CSS；新详情样式单独位于页面样式分片，无新增全局工具类依赖。

部署前后后端指纹相同：`542bae25b5b77d3bc93a0efac12e53e499a0ec874aca4ea35fdee627dc7ef861`。未主动改写业务数据。手机检查属于浏览器响应式验收，非物理手机测试。

本地证据：`.local/track-detail-20260921/`；正式包及回执：`deploy/.release/ui-20260921-183734/`；服务器：`/srv/qianlong-loci/releases/ui-20260921-183734/`。
回退镜像：`loci-qianlong:before-ui-20260921-183734`。
