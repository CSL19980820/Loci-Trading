# UI 收尾与上线验收 · 2026-09-20

## 发布结果

前端版本 `ui-20260920-115124` 已于北京时间 **2026-09-20 11:51:49** 部署至 `my-debian:/srv/qianlong-loci`。站点为 `qianlong.chenkit.cloud`。

| 项目 | 实测结果 |
| --- | --- |
| 当前镜像 | `loci-qianlong:ui-20260920-115124` |
| 镜像 ID | `sha256:73599e7742f537d1a03cfc495f28d9105307458be7e57c417ff3fa02c9591de0` |
| 回滚镜像 | `loci-qianlong:before-ui-20260920-115124` |
| 容器状态 | `healthy`；上线后检查重启计数为 0 |
| 发布范围 | 只覆盖前端静态产物并添加前端发布清单 |
| 前端文件 | 275 个，上传包与镜像内文件逐一校验 SHA256 |
| 后端内容 | 发布前后源码、CLI、模板与 requirements 摘要相同 |
| 数据与配置 | 没有上传本地数据库，没有主动执行数据迁移；原编排不变，只固定新的 `LOCI_IMAGE` |

后端内容摘要：`bca2a5951398bfa1e809f6334f0b15ccab6c16c457e96c03cda00873e7e07981`。
前端 HTML 摘要：`21e363bf9d21ea50795d5ef1b5593d957d7d3e32b70128a596fef9b8a8064d5a`。

当前工作区存在其他未提交改动，因此没有将本地后端代码打包上线，没有执行 Git 提交或推送。原后端镜像和它的启动行为被保留；没有读取或修改用户的生产管理员口令。

## 本轮收尾

**参数编辑和日期。** 策稿台手机端改为完整参数卡片，参数名、类型、默认值、标签、最小值和最大值均可操作，切换分区保留编辑值。桌面表格保留。日期范围通过日历年月控件选择，覆盖完整区间、清空、关闭和焦点归还；修正守护研判区的网格最小宽度，日期控件不再伸出窄屏。

**信息层级。** 整理重复标题、说明段、筛选工具栏与移动端排列；策稿台保留可编辑名称，同时补齐可访问标题与返回工坊入口。持仓信息前置，次要说明收拢。研判页不再把固定的“本金 20 万元”当成当前账户事实。

**确认弹窗。** 修复确认框被抽屉遮罩盖住而无法点击的问题。另修复“放弃”先触发内部关闭事件、把结果提前结算成取消的问题：确认动作现在只结算一次。桌面和手机均验证继续编辑保留内容、明确放弃关闭抽屉、重新打开恢复原配置，并确认该流程不发送保存请求。

**回归证据。** 修正过时的路由、标题和会话夹具；日期与研究证据测试改为操作当前真实控件。研究说明同时验证鼠标悬停和键盘触发，仍断言降级研究不能充当核验证据、来源错误可见、PIT 条件与签署 payload 正确。没有删除这些业务约束来换取通过。

## 发布前验证

下表路径相对于 `frontend/artifacts/ui-20260920/`。

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| 类型检查与生产构建 | 通过 | `release-build.log` |
| 前端单元测试 | 160 个文件、720 条测试通过 | `release-unit.log` |
| Playwright 交互回归 | 22 条通过 | `release-e2e.log` |
| 前端发布保护测试 | 6 条通过 | `deploy-tests-final.log` |
| 新增 Python 文件 Ruff F 检查 | 通过 | 本轮命令回执 |
| PowerShell / Bash 语法 | 通过 | 本轮解析与 `bash -n` 回执 |
| 常规路由、多级分区、四种外观 | 924 个状态，0 个失败 | `final-normal/summary.json` |
| 弹窗与抽屉、多级分区 | 224 个状态，0 个失败 | `dialogs-2/summary.json` |
| 日期与选择框弹层 | 132 个状态，0 个失败 | `controls-2/summary.json` |
| 320 / 768 像素补充检查 | 155 个状态，0 个失败 | `narrow-1/summary.json` |
| 空、错误、加载状态 | 488 个状态，0 个失败 | `degraded-2/summary.json` |

合计 **1,923 个模拟接口界面状态**。这是路由、分区、外观、尺寸和状态的检查记录，不代表 1,923 条独立业务流程，也不是生产账号验收。部分矩阵先于最后的确认框修复完成，最终 22 条生产构建交互回归补充覆盖该修复。

历史尝试中的 `e2e-final.log`、`controls-1`、`dialogs-1`、`degraded-1` 不是最终结果，保留用于排障。一次性改写脚本已归档到 `artifacts/ui-20260920/applied-patches/`，不可重复运行。

## 公网实际验证

脚本 `e2e/ui-production-verify.mjs ui-20260920-115124` **不使用接口模拟或生产凭据**。

HTTP 和 HTTPS 的 `/api/health` 均为 200；`/login` HTML 与发布清单一致。HTTPS 实际下载 **272 个构建资源**，状态码与 SHA256 全部匹配，没有关闭证书验证。390 / 1440 像素浏览器均完成登录页日间与夜间切换，没有文档横向溢出或 JavaScript 页面错误。匿名访问守护智能体页面正确回到登录页，匿名会话保持未登录。

证据：`production/receipt.json`、`production-check.log`、`production/*.png`。服务器直连和 nginx 的 HTML 校验也在发布脚本中通过，结果见 `/srv/qianlong-loci/releases/ui-20260920-115124/DEPLOYMENT.json`，本地副本在 `deploy/.release/ui-20260920-115124/DEPLOYMENT.json`。

## 同镜像真实接口验证

脚本 `e2e/ui-isolated-verify.mjs` 使用与线上完全相同的镜像 ID，独立临时容器、内存数据目录和随机测试口令；未挂载生产数据库，未使用生产环境文件，调度器关闭。

通过实际登录表单取得测试会话，并检查智能体、模型供应商、战法、任务和研究目录的真实接口。390 / 1440 像素下检查 12 个路由 / 分区组合，共 **24 个页面状态**。关闭空库首次启动的初始化提示后验证实际页面、标题、布局，未发现 JavaScript 页面错误。最终候选列表读取为 200，检查窗口的服务端日志没有捕获到 SQLite operational error、ERROR 或 Traceback。

空库首次进入会由应用自身启动行情初始化，接口返回 202；这发生在临时数据环境，不是生产环境的人工同步。登录前的 401 是匿名鉴权结果。首轮临时测试曾记录候选列表 503，后续完整复测未复现，最终候选读取为 200；原始记录仍保留，不把首轮所有接口都表述为成功。

最终证据：`isolated-ui-20260920-115124-final/receipt.json`、`isolated-check-final.log` 和对应截图。临时容器与其内存数据在完成后删除，专属 SSH 隧道关闭。

## 边界与回滚

没有使用用户的真实生产账号执行交易、调用模型、改密、保存配置或创建业务记录。因此，不能把模拟接口回归或隔离测试账号结果宣称为真实生产持仓 / 写操作已验收。

构建仍有大于 500 kB 的分块提示，主要是编辑器相关格式化资源；这是尚未消除的体积提示，不是构建失败。未为消除此提示而把图表和编辑器全部合入首屏。

旧镜像保留，发布脚本具备失败恢复路径，部署保护测试通过；本次成功上线未触发真实回滚，也没有额外中断生产做回滚演练。人工回滚命令及发布范围说明见 `deploy/README.md` 的“仅前端发布”章节。
