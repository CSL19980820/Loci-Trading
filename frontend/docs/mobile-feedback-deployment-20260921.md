# 移动端反馈修复上线验收 · 2026-09-21

已部署：`ui-20260921-105856`；上线时间：`2026-09-21T11:00:36.268071+00:00`；公网复核：`2026-09-21T11:02:12.196129+00:00`。

## 本轮交付

- 首页智能体研判合并盘前、盘中、盘后、周复盘及其他股票智能体记录，按实际生成时间倒序取最新16条。新增只读 `/api/ops/guardian/activity`，先限制元数据，再读取有界正文摘要，不返回决策上下文、分享令牌或报告租约。既有报告列表与交易记录接口保持原契约。
- 持仓曲线使用压缩采样坐标，折叠非交易时段。盘中缺测以虚线连接已知端点，不补造或前向填充估值；回撤纵轴按实际数据范围展开，远处预警线以图外提示保留。最近有效估值不会伪装为当前估值。
- 手机助手的图片、模型、思考、发送使用同一操作行，长模型名称省略；模型与思考使用独立上方选择面板、搜索与供应商分组。打开手机模型菜单不再强制聚焦搜索框弹出键盘；共享 CommandInput 默认行为保持不变。

## 证据

前端构建/类型检查及76项单元测试通过；后端11项测试及Ruff F检查通过。40项实际构建浏览器检查覆盖360×650、390×844、430×932暗色、1440×900桌面，含短可见视口、模型搜索选择、思考选择、图表点选、图片和发送报文。发送被隔离夹具拦截，不调用外部模型。

另外从生产只读取得实际曲线快照，使用当前构建渲染并查看截图；模型和思考面板打开状态也逐一查看。浏览器证据是模拟视口和临时认证环境，不等于所有手机内置浏览器的真机认证。

候选镜像在无网络、无生产数据挂载环境完成真实应用API检查，覆盖新接口鉴权、四阶段、摘要、旧周报重生成排序、参数边界、禁止写入及原有持仓曲线。生产上线后282个公开文件按SHA-256复核，入口不缓存、旧哈希资源保留、缺失分片404不缓存、匿名研判请求401。

生产查询使用SQLite只读连接核验当前最新16条来源；15条盘中记录与1条报告均有正文，并与全局生成时间选出的记录一致。阶段是共同参与排序，不强行给每种阶段预留名额。

## 发布边界及回滚

仅覆盖已验收前端和 `src/ledger/infrastructure/guardian_queries.py`、`src/ops/api/guardian_reads.py`。其他后端源文件指纹与配置保持一致，未迁移数据库、改交易规则、改模型供应商或写入真实账本。

回滚镜像：`loci-qianlong:before-ui-20260921-105856`。发布及验证资料：`/srv/qianlong-loci/releases/ui-20260921-105856/`。本地证据：`.local/mobile-feedback-20260921/` 的 `FINAL_ACCEPTANCE.json`、`DEPLOYMENT.json`、`PUBLIC_VERIFICATION.json`、`API_SMOKE.log`、`browser/report.json`、`VISUAL_EXTRA.json`。

本轮隔离预览已停止；临时数据库、随机测试凭据及原始生产曲线快照已删除。保留已验收的源码/构建、当前发布包和必要截图/日志作为验收及回退依据。
