# shared

跨页面组件、API 客户端、路由、stores、lib。

- `api/palace.ts` — 账本 HTTP
- `api/quant.ts` — 量化 barrel（实现见 `quant_client` / `quant_market` / `quant_strategy` / `quant_ops` / `quant_review`）；调用方仍 `from '@/shared/api/quant'`
